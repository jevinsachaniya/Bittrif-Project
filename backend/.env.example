# Local development only. Copy to backend/.env if you need to override defaults.
# Render supplies PORT automatically. Do not set PORT manually on Render.
DATA_DIR=./data
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
